{
  "name": "webserver",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Dustin Ludwig",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "build:ui": "rm -rf dist && cd ../notes && npm run build && cp -r dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
